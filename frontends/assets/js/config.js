// ====================================
// Configuration
// ====================================

const CONFIG = {
    apiUrl: window.location.origin || 'http://localhost:8001',
    maxConcurrent: 1 // Process one by one
};
